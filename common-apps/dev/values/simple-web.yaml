clusterName: my-cluster

image:
  repository: hashicorp/http-echo
  tag: "0.2.3"
  pullPolicy: IfNotPresent

# ALB Ingress 비활성화 (Istio Gateway 사용)
ingress:
  enabled: false

# Istio Gateway + VirtualService 활성화
istio:
  enabled: true
  host: web.hoonii2.store